# ⏱️ Study Time Tracker (CLI)

![Python](https://img.shields.io/badge/python-3.8+-blue.svg)
![OS](https://img.shields.io/badge/os-linux%20%7C%20macos%20%7C%20windows-orange.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

Консольное приложение на Python для учета времени обучения. Помогает отслеживать прогресс, ставить цели и вести дневник изученного материала.

---

## 🌟 Основные возможности
- **Таймер сессий:** Учет чистого времени работы с возможностью паузы.
- **Дневник обучения:** Автоматическое предложение записать итоги после каждой сессии.
- **Статистика:** Просмотр накопленного времени (день/неделя/месяц).
- **Локальное хранение:** Все данные хранятся в базе SQLite и текстовом логе.

## 📂 Структура проекта
- `study_tracker_console/` — основной пакет с логикой приложения.
- `study_tracker.db` — база данных SQLite (создается автоматически).
- `study_journal.txt` — текстовый файл с заметками (создается автоматически).

## 🛠️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone [https://github.com/ВАШ_ЛОГИН_НА_GITHUB/study_tracker.git](https://github.com/ВАШ_ЛОГИН_НА_GITHUB/study_tracker.git)
cd study_tracker
```

### 2. Создание виртуального окружения
- **Linux/macOS:** `python3 -m venv venv`
- **Windows:** `python -m venv venv`

### 3. Активация окружения
| ОС | Оболочка | Команда |
| :--- | :--- | :--- |
| **Linux / macOS** | **bash / zsh** | `source venv/bin/activate` |
| **Windows** | **PowerShell** | `.\venv\Scripts\Activate.ps1` |

---

## 🚀 Быстрый запуск (Alias) — для удобства
Чтобы не вводить каждый раз путь к файлу и не активировать окружение вручную, можно создать псевдоним (alias). 

### Для Linux (Bash/Zsh):
1. Откройте файл настроек: `nano ~/.bashrc` (или `~/.zshrc`)
2. Добавьте в конец строку (заменив путь на свой):
   ```bash
   alias tracker='/полный/путь/к/проекту/venv/bin/python /полный/путь/к/проекту/study_tracker_console/study_tracker.py'
   ```
3. Обновите настройки: `source ~/.bashrc`
4. Теперь программу можно запустить из любого места просто словом: `tracker`

---

## ⌨️ Использование
Программа работает в интерактивном режиме:
- Нажмите **Enter**, чтобы начать сессию.
- Введите **p**, чтобы поставить на паузу.
- Введите **s** или **stop**, чтобы завершить сессию.

## 📝 Лицензия
MIT — используйте и модифицируйте как хотите!

---
*Разработано для повышения продуктивности и личного прогресса.*
